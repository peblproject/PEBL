// var define = null;

replaceMe

window.PEBL = PEBL;

window.onload = function () {
    PEBL.start(window.PEBLTeacher,
	       function (peblInstance) {
		   window.pebl = peblInstance
	       });
};
